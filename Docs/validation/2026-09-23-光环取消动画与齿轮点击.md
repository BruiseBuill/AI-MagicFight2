# 验证 · 2026-09-23 · M34（光环取消动画 + 齿轮点击 + 知识库）

> 范围：M34 的三条改动。**`Core/` 未改动 → 未重跑万局自测**（规则行为零风险）。
> 限制写在最后：**真机端到端链路仍未跑**。

## 1. 编译

`refresh_unity`（force / all / compile）之后读 Console：**0 error / 0 warning**。

改动都在表现层：`Assets/Scripts/Unity/Ui/` 下的 `AuraIconView.cs`、`PreparedAuraView.cs`、
`HudBuffView.cs`、`HudView.cs`、`BattleUi.cs`、`UiLayout.cs`。
**没有动 Prefab，也没有跑构建菜单** → `Assets/Prefabs/Ui/BattleCanvas.prefab` 与
`SampleScene.unity` 的 mtime 保持 2026-09-23 09:48（构建前），
`m_Script: {fileID: 0}` 计数 **0**（M32 那条「Prefab 被拒绝保存」的检查项）。

## 2. 光环取消：飞回动画

方法：`execute_code` 合成数据定点验证（`BattleDriver.StopBattle()` 冻住每拍重刷，
注入 3 枚指示物、其中 2 枚设为「已准备」），再走真实入口 `BattleUi.OnPreparedAuraCancelled`。

| 时点 | 读到的事实 | 判定 |
|---|---|---|
| 准备后 | HudBuff 可见图标 **1**、准备标记 **2** | ✔ 同一枚只亮一处 |
| 取消当帧 | `ReturningCount=1`、标记 **0**、HudBuff 可见仍 **1**、飞行图标 `flying=true` `scale=1.00`、位置 `(90, 324)` = 它原来的槽位 | ✔ 起飞点正确、HudBuff 里那枚仍隐着 |
| 飞行中 | 位置 `(611.8, 826.9)`，落在起点 `(90,324)` → 终点 `(1022.3, 1222.5)` 的连线上；`scale` 恒 1 | ✔ 是纯位移，不是缩放 |
| 落地（0.3 s 后） | `ReturningCount=0`、`_returningKeys` 空、那一枚挂在 `PlayerGroup/Slot1`、坐标 `(1022.25, 1222.50)` = 默认位第 1 格（`HomeSlotFor` 算出的落点） | ✔ 落在正确的格子、收尾无泄漏 |

截图（`Captures/art-review/`）：
`m34_aura_01_prepared.png`（准备态）· `m34_aura_02_cancelled_still_hidden.png`（取消后、HudBuff 还没放出来）·
`m34_aura_03_midflight.png`（在途一帧，剑 +1 正飞向徽标旁）。

## 3. 齿轮点击

| 检查 | 结果 |
|---|---|
| 对齿轮中心的 `GraphicRaycaster.Raycast` | 只命中 **GearButton**（`ResultRoot`/`PeekLayer` 的遮罩当时是关着的；`PromptBar` 底图与文字 `raycastTarget=false`）→ **不是遮挡** |
| 点击区 vs 烘在 `Hud_Bar.png` 上的齿轮 | `x 617…662 / y 22…67` vs 亮部 `x 619…663 / y 24…68`（中心差 ~1.5 px）→ **不是热点错位** |
| `gear.onClick` 运行时监听器 | 修前 **0** → 修后 **1**（`m_Calls.m_RuntimeCalls`） |
| 真事件派发 `ExecuteEvents.pointerClickHandler` | 种子 `694248225 → 2047714048`，`IsRunning` `False → True` → **确实重开了一局** |

## 4. 知识库

| 检查 | 结果 |
|---|---|
| `Docs/` 体积 | **271.5 MB → 1.3 MB**（92 个文件 / 285 MB 截图迁到 `Captures/art-review/`；文件一个没删） |
| 二进制残留 | `Docs/` 内已无 `.png`（只剩 `art-review/README.md` 指路） |
| 引用一致性 | 全库检索 `art-review`：只剩「说明这次迁移」的几处文字（本文件 / `00-当前基线.md` / `M34 实现说明` / `项目目录规范` / `05-决策记录` / `art-review/README.md`）—— 功能性引用为 0 |
| 工具脚本 | `Tools/font-audit/render_font_samples.py`、`Tools/art-audit/render_art_sheets.py`、`Tools/art-audit/slice_icons.py` 的输出目录 / 注释都已改指 `Captures/art-review/` |
| 新增入口 | `Docs/00-当前基线.md`、`Docs/implementation/README.md`；`Docs/README.md` 重写为三层读法 |
| 状态头 | 25 篇实现记录已带 `> **性质**：现行 / 历史 · **最后核对** 2026-09-23` |

## 5. 限制（不要当成已验证）

1. ⚠ **真机端到端链路仍未跑**：在**真实对局**里用真人拖拽走完
   「拖光环 → 飞到手牌区左侧 → 再拖取消 → 滑回默认位」，以及「点齿轮重开」的**手指级**点击。
   本次光环部分是**合成数据**（引擎在飞行的 0.3 s 窗口内不会插进别的状态），
   齿轮部分是**事件级**（与 EventSystem 派发同一个 handler，但没经过鼠标 down/up 的完整时序）。
2. 齿轮的「设置面板」仍不存在（M10 未做）—— 现在的语义是「重开一局」。
