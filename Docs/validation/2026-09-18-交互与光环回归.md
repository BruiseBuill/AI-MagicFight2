# 2026-09-18 · 交互与光环回归

项目：`E:\UnityProject\Unity_AI_CardFight2`。Unity `2022.2.15f1c1`。

## 完成的修改

- 14 份项目文档按规则、工程、实现、验证分类，新增文档入口、目录规范、MCP 运维指南，修复引用。
- 保留进攻和防御必须拖动的入口，恢复换牌及效果选手牌目标的点击操作。
- 防御加值光环改为逐枚主动消耗；未使用的光环不计入防御预算。
- 所有符合阈值的免疫来源均可选；免疫直接成功，无需出牌，覆盖双发与未防御额外伤害。
- 连击光环不满足攻击力量门槛时不提供使用选项。
- 敌方和不可用光环禁止拖动，仍可长按查看；每枚指示物独立显示，消耗或失效同步移除。
- `AuraTooltipView` 拆为独立脚本，修复预制体与场景接线，删除遗留丢失组件；详情浮层定位到图标底边。

## 自动验证

| 验证 | 结果 |
|---|---|
| Unity EditMode | **23/23 通过**，0 失败，0 跳过 |
| 新增光环回归 | 15 项，含高低阈值边界、双发、空手免疫、未防御额外伤害、多个免疫来源、跳过不扣光环、双光环逐枚使用、回手失效、连击门槛、启用光环的 AI 对局 |
| 原有 CoreSmokeTests | 8 项通过 |
| .NET 规则自测 | **10,000 局通过**，0 异常、0 失败项；先手胜 5,058，后手胜 4,942，平均 4.31 回合，最长 16 回合 |
| 场景与预制体 | 丢失脚本均为 0，双方 HudBuff 与 Tooltip 引用有效 |
| 最终 Play 模式 Console | 0 错误、0 警告 |
| MCP 启动入口 | 用户提供的离线 uvx 命令加 `--help` 返回 0，参数有效；启动脚本识别 8080 已占用并正常退出 |

原始输出：[规则自测](2026-09-18-rule-selftest.txt)、[Unity 测试结果](2026-09-18-editmode.json)。

## Play 模式交互检查

在实际场景中通过 MCP 调用运行中的 Button 和指针处理器，使用可控局面验证；长按通过设置按下时间并执行 Update 验证。此项验证不是人工鼠标实机操作，也不覆盖移动设备触摸输入。

| 行为 | 结果 |
|---|---|
| 进攻/防御单击手牌 | 均不提交决策 |
| 手牌拖出有效区域 | 不提交 |
| 手牌拖入中央出牌区域 | 提交正确选项 |
| 光环拖到手牌区之外 | 不提交 |
| 可用己方光环拖入手牌区 | 提交正确选项 |
| 敌方、不可用光环 | 拖动被拒绝 |
| 长按己方/敌方图标 | 显示对应内容，松手隐藏 |
| 相同光环独立显示与消耗 | 玩家 2 枚 + 敌人 1 枚显示 3 图标，消耗 1 枚后显示 2 图标 |
| 左右归属 | PlayerGroup 在左，EnemyGroup 在右 |
| 来源冷却完成回手 | 对应图标移除 |

探针保存在 `Tools/QA/*.cs.txt`。仅用于临时 Play 对局，执行后退出 Play，不保存测试局面。

截图：[光环长按详情](../../Captures/2026-09-18-aura-regression/hudbuff-long-press-1.png)。截图中的卡牌为交互验证布置的局面。

## 交付状态

已保存 `BattleCanvas.prefab` 与 `SampleScene.unity`，编辑器退出 Play。未生成发布构建。
修改前备份：`E:\UnityProject\_CardFight2_Backups\before-20260918-aura-130747.zip`。
