# 文档与资源结构整理

> 状态：验证记录 · 执行日期：2026-09-25 · 范围：目录、引用、工具路径与 Unity 资产加载

## 调整内容

- 根 README 改为仓库相对入口；Docs 按当前规范、实现快照、历史档案、验证和模板导航。
- 7 篇已标为历史的初版实现移入 `Docs/archive/implementation/`；当前交互总述移入 `Docs/design/战斗交互.md`。
- 审计输出和原始验证结果移入 `Artifacts/`；一次性清理脚本移入 `Tools/archive/`；截图统一进入 `Captures/`。
- `Assets/Font/` → `Assets/Art/Fonts/`；全局 URP 设置从 Assets 根移入 `Assets/Settings/`。两次移动均通过 Unity `AssetDatabase.MoveAsset` 执行。
- 同步 5 个编辑器构建器中的字体 / 报告路径；25 个资源工具移除旧工程硬编码或调整输入输出。
- 增加文档规范、模板、目录职责与资源关系表、格式约定、仓库检查脚本；补回 `ArchitectureSmoke.csproj` 并纳入 Git。

精确文件映射和校验结果见 [迁移完整性](../../Artifacts/validation/2026-09-25-layout/migration-integrity.json)。
既有用户修改的 3 个 UI 脚本与 MCP 启动脚本保持原字节；当前基线、第 24 号说明和实现索引中的既有第 39 号补丁记录继续保留。

## 环境

- Unity `2022.2.15f1c1`，目标工程经 MCP `project/info` 确认。
- Python `3.12.10` 用于标准库检查；资源预览依赖见 [工具说明](../../Tools/README.md)。
- 系统 PATH 中 .NET SDK 为 `7.0.102`，自测项目要求 .NET 9。
- 工作区开始时已有未提交修改，本轮未提交 Git、未重建 UI、未进入 Play。

## 已执行的检查

| 检查 | 结果 | 证据 / 限制 |
|---|---|---|
| 仓库文档与工具检查 | 通过 | [repo-check.json](../../Artifacts/validation/2026-09-25-layout/repo-check.json)；检查本地文件链接、索引、旧路径与 Python 语法，准确数量以报告为准 |
| 检查器自身的回归 | 6 项通过 | `python -m unittest discover -s Tools/Repo -p test_*.py -v`；覆盖链接、示例排除、目录逃逸、未索引文档、旧路径与语法错误 |
| Unity 资源 / meta 配对 | 通过 | `python Tools/art-audit/verify_layout.py`；包含目录 `.meta` |
| GUID 唯一性 | 692 个，重复 0 组 | 迁移前后全部 692 个 `.meta` 哈希一致 |
| 被移动资源内容 | 77 个文件全部一致 | 字体目录内容及 URP 设置逐文件 SHA-256 对比 |
| 序列化引用解析 | 无新增或未记账悬空引用 | 扫描 Assets + ProjectSettings，含包 GUID；第三方已知问题见下方 |
| 卡图命名与数量 | 两组各 40 张，01–40 连续 | 完整卡面与无字插画分别检查 |
| Unity 刷新与编译 | 编辑器恢复 ready，Console error 为 0 | 未进入 Play；没有重建场景或 Prefab |
| Unity 加载入口 | 6 个 Resources 键、7 个资产路径全部可加载 | [加载记录](../../Artifacts/validation/2026-09-25-layout/unity-asset-loads.json)；场景未脏，编辑器未播放 |
| 历史原始产物 | 内容全部保留 | 迁移 JSON / TXT / CSV / Markdown / HTML 与原件逐字节比较 |
| 图标源表预览 | 通过 | 使用带 numpy / Pillow 的运行环境执行 `slice_icons.py`，解析 3 个图标块；未传 `--apply` |
| 缺失源图的显式输入 | 帮助入口通过 | `slice_handpick_kit.py --help` 显示 `--source` / `--apply`；未重新生成切片 |
| .NET 架构冒烟 | 未运行成功 | `dotnet run --project Tools/ArchitectureSmoke` 返回 `NETSDK1045`：本机 SDK 7 不支持目标 net9.0 |

## 已知限制

- `Assets/ThirdParty/MagicCardKit/Prefabs/BigMagicCard.prefab` 原有 TMP emoji Sprite Asset GUID 未随包提供，检查器单独列为已知问题；本次没有掩盖为已修复。
- 不宣称完成 Play、真人拖拽、视觉验收或多局规则回归；本轮不涉及玩法修改，历史结果保持原日期。
- .NET 验证需要本机提供 .NET 9 SDK 后运行；未修改机器 SDK 或降低项目目标框架。
- `slice_handpick_kit.py` 旧源图已不在仓库，新增 `--source` 输入；不能凭现有切片反向声称源图已恢复。
- 外部网站、Markdown 标题锚点和历史原始报告内部路径不在仓库链接检查范围内。
- 仓库是 `blob:none` 部分克隆，`git diff` 所需的远端对象无法通过当前网络获取；差异审查改用本轮开始时保存的工作文件快照。已确认 C# 仅有 5 个编辑器文件的路径常量变化、原有补丁说明保留，见 [本地差异核对](../../Artifacts/validation/2026-09-25-layout/local-diff-review.json)。

## 后续维护

新增文档遵循 [文档规范](../engineering/文档规范.md)，新增资源遵循 [目录规范](../engineering/项目目录规范.md)。
切图工具依赖原始素材与 Python 图像库；正式验证输出按日期新增，临时结果写入 `Artifacts/work/`。
